name: CI/CD

on:
  push:
    branches:
      - genesis
      - master

jobs:
  build:
    name: Build project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: npm && npm run build
